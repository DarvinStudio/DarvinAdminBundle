parameters:
    darvin_admin.admin.listener.class: Darvin\AdminBundle\EventListener\AdminListener

services:
    darvin_admin.admin.listener:
        class: %darvin_admin.admin.listener.class%
        arguments:
            - "@security.encoder_factory"
        tags:
            - { name: doctrine.event_listener, event: onFlush }
