darvin_admin:
    cache:
        clear:
            sets:
                widget:
                    commands:
#                        doctrine_result: '@doctrine.cache_clear_result_command'
                list:
                    commands:
#                        doctrine_result: '@doctrine.cache_clear_result_command'
