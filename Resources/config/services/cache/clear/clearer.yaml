parameters:
    darvin_admin.cache.clearer.class: Darvin\AdminBundle\Cache\Clear\CacheClearer

services:
    darvin_admin.cache.clearer:
        class: '%darvin_admin.cache.clearer.class%'
        arguments:
            - '@kernel'
        calls:
            - [ setLogger, [ '@?logger' ] ]
