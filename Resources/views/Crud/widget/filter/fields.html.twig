{% extends 'form_div_layout.html.twig' %}

{%- block form_row -%}
    <div class="filter_element">
        {{ form_label(form) }}
        {{ form_errors(form) }}
        {{ form_widget(form) }}
    </div>
{%- endblock form_row -%}
