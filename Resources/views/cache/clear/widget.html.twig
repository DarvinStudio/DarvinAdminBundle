{% trans_default_domain 'admin' %}

<div class="header__btn-cache">
    {{ form_start(form, {'attr': {'class': 'js-form'}}) }}
        {{ form_widget(form) }}
        <button onclick="return confirm('{{ ('cache.action.clear.confirm')|trans|e('js') }}');" type="submit" title="{% trans %}cache.action.fast_clear.title{% endtrans %}">
            <span>{% trans %}cache.action.fast_clear.submit{% endtrans %}</span>
        </button>
    {{ form_end(form) }}
</div>
