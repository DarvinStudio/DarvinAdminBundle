{% trans_default_domain 'admin' %}

<div class="header__btn-cache">
    {{ form_start(form, {'attr': {'class': 'js-form'}}) }}
        {{ form_widget(form) }}
        <button type="submit"
                onclick="return confirm('{{ ('cache.clear.widget.form.confirm')|trans|e('js') }}');"
                title="{% trans %}cache.clear.widget.form.help{% endtrans %}">
            <span>{% trans %}cache.clear.widget.form.submit{% endtrans %}</span>
        </button>
    {{ form_end(form) }}
</div>
