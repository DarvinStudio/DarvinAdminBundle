{% extends 'form_div_layout.html.twig' %}

{%- block collection_widget -%}
    {% if attr.class is defined and attr.class %}
        {%- set attr = attr|merge({'class': attr.class ~ ' collection'}) -%}
    {% else %}
        {%- set attr = attr|merge({'class': 'collection'}) -%}
    {% endif %}
    {% if allow_add %}
        {%- set attr = attr|merge({'data-allow-add': 1}) -%}
    {% endif %}
    {% if allow_delete %}
        {%- set attr = attr|merge({'data-allow-delete': 1}) -%}
    {% endif %}
    {% if prototype is defined %}
        {%- set attr = attr|merge({'data-prototype': form_row(prototype) }) -%}
    {% endif %}
    {{- block('form_widget') -}}
{%- endblock collection_widget -%}

{% block darvin_admin_dropzone_widget %}
    {% if form.vars.data %}
        {% for image in form.vars.data if image.id %}
            <a href="{{ image | image_original }}"><img src="{{ image | image_crop('admin') }}"></a>
        {% endfor %}
    {% endif %}
    {{ form_widget(form) }}
{% endblock %}

{% block darvin_image_image_widget %}
    {% if form.vars.data and form.vars.data.id %}
        <a href="{{ form.vars.data | image_original }}"><img src="{{ form.vars.data | image_crop('admin') }}"></a>
    {% endif %}
    {{ form_widget(form) }}
{% endblock %}

{%- block form_label -%}
    {% if label is not sameas(false) -%}
        {% if not compound -%}
            {% set label_attr = label_attr|merge({'for': id}) %}
        {%- endif %}
        {% if required -%}
            {% set label_attr = label_attr|merge({'class': (label_attr.class|default('') ~ ' required')|trim}) %}
        {%- endif %}
        {% if label is empty -%}
            {%- if label_format is not empty -%}
                {% set label = label_format|replace({
                '%name%': name,
                '%id%': id,
                }) %}
            {%- else -%}
                {% set label = name|humanize %}
            {%- endif -%}
        {%- endif -%}
        <label{% for attrname, attrvalue in label_attr %} {{ attrname }}="{{ attrvalue }}"{% endfor %}>
            {{ translation_domain is sameas(false) ? label : label|trans({}, translation_domain) }}
            {% if darvin_admin_info %}<span>{{ darvin_admin_info|trans({}, translation_domain) }}</span>{% endif %}
        </label>
    {%- endif -%}
{%- endblock form_label -%}
