{% form_theme form '@DarvinAdmin/fields.html.twig' %}

{% trans_default_domain 'admin' %}

{{ form_start(form, {'attr': {'class': 'js-form'}}) }}
<div class="form-product-show">
    {{ form_widget(form) }}
    <button class="form-product-show__btn-save-center" type="submit">{% trans %}image.action.edit.submit{% endtrans %}</button>
</div>
{{ form_end(form) }}
