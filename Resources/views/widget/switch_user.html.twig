{% trans_default_domain 'admin' %}

{% if user != app.user %}
    <a href="{{ path('darvin_admin_homepage', {'_switch_user': user.username}) }}"
       class="edit_element switch_user">
        <span>{% trans %}security.action.switch_user.link{% endtrans %}</span>
    </a>
{% endif %}
